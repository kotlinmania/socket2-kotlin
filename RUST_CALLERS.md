# Rust callers — `socket2-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `socket2` | `socket2` | 12 | 5 | 5 | 7 |

## `socket2`

### Inbound symbols (9 distinct, 12 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `socket2`.

#### `socket2::Socket` — 3 site(s) (2 codex, 1 other, 2 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/escalate_server.rs:11` — `use socket2::Socket`
- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:8` — `use socket2::Socket`
- `tungstenite-kotlin/tungstenite` `tests/connection_reset.rs:12` — `use socket2::Socket`

#### `socket2::Domain` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:4` — `use socket2::Domain`

#### `socket2::MaybeUninitSlice` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:5` — `use socket2::MaybeUninitSlice`

#### `socket2::MsgHdr` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:6` — `use socket2::MsgHdr`

#### `socket2::MsgHdrMut` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:7` — `use socket2::MsgHdrMut`

#### `socket2::Type` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:9` — `use socket2::Type`

#### `socket2::SockAddr` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `dns-lookup-kotlin/dns-lookup` `src/addrinfo.rs:1` — `use socket2::SockAddr`
- `dns-lookup-kotlin/dns-lookup` `src/nameinfo.rs:1` — `use socket2::SockAddr`

#### `::socket2` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `rama-net-kotlin/rama-net` `src/socket/mod.rs:1` — `pub_use ::socket2 as core`

#### `socket2::TcpKeepalive` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `tonic-kotlin/tonic` `src/transport/server/incoming.rs:7` — `use socket2::TcpKeepalive`
