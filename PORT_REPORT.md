# Port report — `socket2-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `socket2`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 12 `use` sites from 5 other repos
- **Outbound**: 161 `use` sites to 2 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `socket2` | `socket2` | 12 | 2 | COD-139 | 8 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (12 edges, 5 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-shell-escalation` | 7 | ✅ |
| `dns-lookup-kotlin` | `dns-lookup` | 2 |  |
| `rama-net-kotlin` | `rama-net` | 1 |  |
| `tonic-kotlin` | `tonic` | 1 |  |
| `tungstenite-kotlin` | `tungstenite` | 1 |  |

### Symbol-level demand (9 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `socket2::Socket` | 3 | 2 | 1 | 2 |
| `socket2::Domain` | 1 | 1 | 0 | 1 |
| `socket2::MaybeUninitSlice` | 1 | 1 | 0 | 1 |
| `socket2::MsgHdr` | 1 | 1 | 0 | 1 |
| `socket2::MsgHdrMut` | 1 | 1 | 0 | 1 |
| `socket2::Type` | 1 | 1 | 0 | 1 |
| `socket2::SockAddr` | 2 | 0 | 2 | 1 |
| `::socket2` | 1 | 0 | 1 | 1 |
| `socket2::TcpKeepalive` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 9 symbols)

##### `socket2::Socket` (3 sites, 2 codex, 1 other, 2 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/escalate_server.rs:11` — `use socket2::Socket`
- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:8` — `use socket2::Socket`

Other workspace call sites:

- `tungstenite-kotlin/tungstenite` `tests/connection_reset.rs:12` — `use socket2::Socket`

##### `socket2::Domain` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:4` — `use socket2::Domain`

##### `socket2::MaybeUninitSlice` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:5` — `use socket2::MaybeUninitSlice`

##### `socket2::MsgHdr` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:6` — `use socket2::MsgHdr`

##### `socket2::MsgHdrMut` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:7` — `use socket2::MsgHdrMut`

##### `socket2::Type` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-shell-escalation` `src/unix/socket.rs:9` — `use socket2::Type`

##### `socket2::SockAddr` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `dns-lookup-kotlin/dns-lookup` `src/addrinfo.rs:1` — `use socket2::SockAddr`
- `dns-lookup-kotlin/dns-lookup` `src/nameinfo.rs:1` — `use socket2::SockAddr`

##### `::socket2` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `rama-net-kotlin/rama-net` `src/socket/mod.rs:1` — `pub_use ::socket2 as core`

##### `socket2::TcpKeepalive` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `tonic-kotlin/tonic` `src/transport/server/incoming.rs:7` — `use socket2::TcpKeepalive`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `libc-kotlin` | 79 | 79 | `libc` | COD-75 | 3 | To Do |
| `windows-sys-kotlin` | 82 | 82 | `windows-sys` | COD-192 | 4 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
