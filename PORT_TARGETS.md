# Port targets — `socket2-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `socket2` that codex-rs uses, and therefore the minimum subset that `socket2-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-139] (rank `8`, tier `direct`, status `To Do`)

**Cargo package name**: `socket2`
**Rust module name (in `use` statements)**: `socket2`

## Summary

- **6** distinct symbols imported by codex-rs
- **7** total `use` statements
- **2** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `socket2::Socket`

- **2 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/escalate_server.rs:11` (`codex-shell-escalation`) — `use socket2::Socket`
- `shell-escalation/src/unix/socket.rs:8` (`codex-shell-escalation`) — `use socket2::Socket`

### `socket2::Domain`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:4` (`codex-shell-escalation`) — `use socket2::Domain`

### `socket2::MaybeUninitSlice`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:5` (`codex-shell-escalation`) — `use socket2::MaybeUninitSlice`

### `socket2::MsgHdr`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:6` (`codex-shell-escalation`) — `use socket2::MsgHdr`

### `socket2::MsgHdrMut`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:7` (`codex-shell-escalation`) — `use socket2::MsgHdrMut`

### `socket2::Type`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-shell-escalation`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `shell-escalation/src/unix/socket.rs:9` (`codex-shell-escalation`) — `use socket2::Type`
